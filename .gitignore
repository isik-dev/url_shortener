# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Cloudflare Tunnel credentials (secret; lives on the Pi only).
# Contains the TunnelSecret - never commit. This repo is PUBLIC.
cloudflared-credentials.json
